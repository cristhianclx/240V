python3 -m venv venv # 1st time

source venv/bin/activate # always
pip install -r requirements.txt # always

django-admin startproject project # 1st time

python project/manage.py runserver # when you want to run this